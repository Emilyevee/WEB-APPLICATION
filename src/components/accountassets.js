import React from 'react'

function accountassets( { image}) {
    return (
        <div className="accountAssets">
            style={ {backgroundImage: image }}

            
        </div>
    );
}

export default accountassets
